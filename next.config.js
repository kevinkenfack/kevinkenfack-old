/** @type {import('next').NextConfig} */
const nextConfig = {
  // Vos configurations Next.js ici
  images: {
    domains: ['avatars.githubusercontent.com'], // Si vous utilisez des images externes
  },
  // Autres configurations...
}

module.exports = nextConfig 